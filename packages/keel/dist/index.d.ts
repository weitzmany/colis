/**
 * @colis/keel — Angular component library
 *
 * Phase 1: Foundation MVP — no components yet.
 * Components will be exported from this file in Phase 2+.
 *
 * CSS token contract:
 * All keel components consume --keel-* CSS custom properties.
 * See src/tokens/defaults.css for the full token list and fallback values.
 * See docs/reference/KEEL_HULL_TOKEN_CONTRACT.md for the shared contract.
 *
 * Usage (Phase 1):
 *   Import the token defaults to get keel's built-in design values:
 *   @import '@colis/keel/src/tokens/defaults.css';
 *
 * Usage with @colis/hull (Phase 2+):
 *   @import '@colis/hull';
 *   Hull will override the defaults with a full theming system.
 *
 * Phase 2+ components will be imported like:
 *   import { KeelButtonComponent } from '@colis/keel';
 */
export declare const KEEL_VERSION = "0.1.0";
//# sourceMappingURL=index.d.ts.map