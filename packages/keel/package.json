{
  "name": "@colis/keel",
  "version": "0.4.0",
  "description": "Angular component library for the Colis ecosystem — standalone, themeable UI primitives",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "ngc -p tsconfig.json",
    "build:watch": "ngc -p tsconfig.json --watch",
    "test:angular-metadata": "node scripts/verify-angular-metadata.mjs",
    "test:button-hover": "node scripts/verify-button-hover.mjs",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build && npm run test:angular-metadata && npm run test:button-hover",
    "release:patch": "npm version patch && npm run clean && npm run build && npm publish",
    "release:minor": "npm version minor && npm run clean && npm run build && npm publish",
    "release:major": "npm version major && npm run clean && npm run build && npm publish"
  },
  "keywords": [
    "keel",
    "angular",
    "components",
    "ui",
    "design-system",
    "colis"
  ],
  "author": "Colis",
  "license": "MIT",
  "peerDependencies": {
    "@angular/common": ">=17.0.0",
    "@angular/core": ">=17.0.0",
    "@angular/forms": ">=17.0.0"
  },
  "devDependencies": {
    "@angular/compiler-cli": "^21.0.0",
    "@angular/forms": "^21.0.0",
    "@types/node": "^20.10.6",
    "prettier": "^3.1.1",
    "rimraf": "^6.1.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
