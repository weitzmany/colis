{
  "name": "@colis/keel-mcp",
  "version": "0.1.5",
  "description": "MCP server for @colis/keel and @colis/hull — gives AI assistants authoritative component and theming knowledge",
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "exports": {
    ".": "./dist/server.js"
  },
  "bin": {
    "keel-mcp": "./dist/server.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "release:patch": "npm version patch && npm run clean && npm run build && npm publish",
    "release:minor": "npm version minor && npm run clean && npm run build && npm publish",
    "release:major": "npm version major && npm run clean && npm run build && npm publish"
  },
  "keywords": [
    "keel",
    "hull",
    "mcp",
    "model-context-protocol",
    "angular",
    "design-system",
    "ai",
    "colis"
  ],
  "author": "Colis",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "peerDependencies": {
    "@colis/keel": ">=0.1.0"
  },
  "peerDependenciesMeta": {
    "@colis/hull": {
      "optional": true
    }
  },
  "devDependencies": {
    "typescript": "~5.3.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
